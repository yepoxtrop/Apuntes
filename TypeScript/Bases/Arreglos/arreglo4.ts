//Tuplas con nombre
let carro: [
    marca:string, //valor para cada indice, es mas referencial
    modelo:string,
    kilometraje:number,
    vertifiado:boolean
] = ["kia", "kia-789", 10, true]
console.log(carro);