function contdor() {
    "use strict"; // El modo estricto se aplica solo dentro de esta función

    y = 2; // Esto generará un error porque 'y' no está declarada
    console.log(y); // No se ejecutará debido al error anterior
}